import { TextReplacePipePipe } from './text-replace-pipe';

describe('TextReplacePipePipe', () => {
  it('create an instance', () => {
    const pipe = new TextReplacePipePipe();
    expect(pipe).toBeTruthy();
  });
});
